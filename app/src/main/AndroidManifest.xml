<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ahmadabuhasan.volleygsonxampp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_indonesia"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_indonesia_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VolleyGsonXampp"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TambahActivity"
            android:label="Tambah Barang"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".UbahActivity"
            android:label="Ubah Barang"
            android:windowSoftInputMode="stateHidden" />
    </application>

</manifest>